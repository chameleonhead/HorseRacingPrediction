# 開催日再取得を単一ジョブで一括実行する

- Status: Completed
- Owner: HorseRacingPrediction maintainers
- Created: 2026-09-09
- Updated: 2026-09-09

## Context

現行の `RaceDayReacquisition` はJRA公式情報から指定日のレースを発見した後、レースごとの `RaceReacquisition` 集約子ジョブを登録する。各子ジョブは独立したブラウザーセッションを作り、開催選択から対象レースまでの公式導線を再探索するため、1日分の完了に時間がかかる。

利用者は、レース別ジョブへ分割せず、指定日全体を一括したジョブとして実行することを希望している。

## Goals

- `RaceDayReacquisition` 1件の実行内で、その日の全開催場・全レースを再取得する。
- 1つのブラウザーセッションを再利用し、レースごとのセッション起動と共通導線探索を削減する。
- 1レースの取得失敗で残りのレースを中断しない。
- 完了を「開催なし」または「当該日の全レース取得成功」に限定し、部分成功を完了として扱わない。
- 同じ日付の有効な依頼に対する既存の重複抑止、保留、停止、監査を維持する。

## Non-goals

- 通常のレース単位再取得操作を削除すること。
- 複数日・期間指定を追加すること。
- Collector/Lambdaの実行時間上限を変更すること。
- 複数Workerによる同一開催日の並列取得。

## Experience and interaction design

管理画面の「開催日を再取得」と日付入力は維持する。確認文から「レース単位のジョブへ分割する」を削除し、「1件のジョブで開催日全体を順次取得する」と説明する。

送信後は従来どおり `RaceDayReacquisition` の詳細へ遷移する。レース別の子ジョブ一覧は表示されず、日付ジョブ自身の実行中・完了・失敗・保留状態を確認する。完了表示は「対象日にJRA開催がない」または「当該日の全レース取得が成功した」のどちらかに限定する。失敗時の詳細には、未取得または失敗した競馬場・レース番号・処理種別をまとめて表示する。

## Documentation updates

- `docs/22-collector-design.md`: 開催日再取得の正本を、子ジョブ分割から単一セッション・単一ジョブ実行へ変更する。
- `docs/20-admin-ui-design.md`: 確認文、遷移先、進捗・失敗表示を単一ジョブの体験へ変更する。
- 開催日ジョブ実行中の更新資格情報と競合拒否は [レース収集リース中の更新をバッチに限定する](../20260909_race-mutation-lease-guard/README.md) に従う。
- 既存の `docs/changes/20260909_race-day-reacquisition/README.md` は実装当時の履歴として書き換えず、本記録が子ジョブ分割の決定を置き換える。

## Technical impact

- `ExecuteRaceDayReacquisitionAsync` は、公式レース発見後に `ScheduleJobAsync(RaceReacquisition)` と `WaitForCollectionDependenciesAsync` を呼ばない。
- 公式レース発見と全レース再取得で同じ `IJraSession` を使用する。
- 日付ジョブのジョブIDとリーストークンを全レースの書き込みへ伝播し、Apiは当該開催日を対象とする有効なバッチ資格情報として検証する。
- 登録済みレースIDとの照合は日付ジョブ内で一度だけ行う。未登録レースには現行どおり決定論的IDを使用する。
- 各レースについて、公式掲載期間内なら出馬表、当日以前なら結果・天候・馬場・払戻を既存workflowで更新する。
- レース単位の非致命的エラーは対象識別子付きで収集し、残りのレースを継続する。全対象の処理後、エラー、未公開、未取得が1件でもあれば日付ジョブを完了させず失敗として報告する。
- 公式開催日程の確認結果、対象日に開催がない場合だけ対象0件で正常完了する。開催があるのにレース一覧または必要なレースデータを取得できず対象0件となった場合は失敗とする。
- 既存の14分内部期限を超えた場合はタイムアウトとして失敗し、収集全体停止の現行契約に従う。期限延長や実行分割による自動再開は本変更に含めない。

## Decisions

### 採用: 単一ジョブ・単一セッション・逐次取得

利用者が求める一括実行を満たし、時間増加の主因であるセッション作成と共通ナビゲーションの反復を除く。JRAへの同時アクセスを増やさないため、同一ジョブ内の並列取得は行わない。

### 採用: 部分失敗後も残りを継続し、最後にジョブを失敗させる

途中の1レースで日付全体が未処理になることを防ぎつつ、「全レース再取得」の一部欠落を成功として隠さない。再実行は同じ日付ジョブの新しい試行として、冪等な更新を日付全体へ再適用する。

### 完了条件

`RaceDayReacquisition` を完了へ遷移できるのは、次のいずれかだけとする。

1. JRA公式開催日程で対象日に開催がないことを確認できた。
2. JRA公式開催日程・レース一覧で特定した当該日の全レースについて、対象日に必要な出馬表または結果データをすべて取得・保存できた。

開催有無を確認できない、開催があるのにレース一覧を確定できない、1レースでも未公開・未取得・保存失敗がある、または期限内に全件を処理できない場合は完了条件を満たさない。

### 不採用: レース別子ジョブを維持して優先度だけ上げる

セッション起動と導線探索の反復が残り、利用者が指定した一括ジョブにならない。

### 不採用: 開催場ごとの並列処理

短縮余地はあるが、一括した単一処理の単純性を損ない、JRAへの同時アクセス量とブラウザー資源使用を増やす。

## Acceptance criteria

1. 開催日再取得の依頼1件につき、`RaceDayReacquisition` 以外の子ジョブを作成しない。
2. 公式開催日程からDB未登録分を含む全開催場・全レースを発見する。
3. 発見と全レース取得に1つのブラウザーセッションを再利用する。
4. 公式掲載期間内の出馬表と、当日以前の結果・天候・馬場・払戻を既存の更新契約どおり保存する。
5. 1レースが非致命的エラー、未公開または未取得でも後続レースを処理し、対象を日付ジョブのエラーへ集約する。
6. JRA公式開催日程で開催なしを確認できた場合、または公式に特定した当該日の全レースを取得・保存できた場合だけ日付ジョブを完了にする。
7. 開催有無・全対象を確定できない場合や、1レースでも未取得・保存失敗がある場合は完了にしない。
8. 同日付の有効依頼の重複抑止、保留、全体停止、監査、リランが維持される。
9. 通常のレース単位再取得機能に回帰がない。
10. 管理画面は単一ジョブで実行することと厳密な完了条件を説明し、子ジョブ進捗を案内しない。
11. Collector/API/UIの関連テスト、solution build、`git diff --check` が成功する。

## Delivery plan

1. 日付再取得の発見処理を既存セッション受け取りへ整理する。
2. レース単位更新を既存セッションで呼べる内部処理へ整理し、日付ジョブから全対象を逐次実行する。
3. 子ジョブ非生成、セッション再利用、部分失敗継続、完了・失敗を自動テストする。
4. 管理画面説明、正本文書、検証記録を同期する。

## Verification record

- Solution build成功（警告0、エラー0）。関連テスト4件とCollector全124件が成功した。

## Deviations and follow-up

- 設計どおり単一ジョブ・単一セッションで実装済み。14分以内に最大開催日の処理が完了するかは実サイト相当検証で計測し、超過する場合は期限または再開方式を別change recordで扱う。
