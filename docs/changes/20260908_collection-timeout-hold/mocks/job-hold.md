# 実行保留の画面案（未承認）

## 一覧・1280px・タイムアウトによる全体保留

```text
収集ジョブ                                  [再開]
収集を保留中
理由: 制限時間内に収集が終わりませんでした
原因ジョブ: 9月6日の結果収集（詳細へのリンク）
開始済みの処理は結果を保存します。新しい処理は開始しません。

[要対応] [処理中] [待機中] [保留中] [最近完了] [すべて]
検索・対象日・状態フィルター
結果収集 / 9月6日               失敗
結果収集 / 9月5日               待機中・個別保留
```

## 詳細・1280px・未開始ジョブ

```text
収集ジョブ > 結果収集
9月5日の結果収集      待機中        [このジョブを保留]
対象・次回実行時刻・親子ジョブ
試行履歴 / 操作履歴
```

保留後は「個別保留」と「保留を解除」を表示する。集約親の場合は未開始の子も保留対象と明記する。実行中は「開始済みのため個別保留できません」と表示する。失敗ジョブにはリランを提供する。

## 360pxと各状態

タイトル、全体状態、理由、操作の順に縦積みする。操作とビュー切替は折り返し、横スクロールを必要としない。FluentButton/既存の状態表示を利用する。

- 読み込み中は状態不明のまま操作を有効化しない。
- 空一覧でも全体状態と再開操作を維持する。
- 送信中は重複送信を抑止する。
- 競合時は最新状態を表示し、開始済みなどの理由を説明する。
- エラー時は操作付近にメッセージを表示し、成功したと表示しない。
- Tab/Enterで操作でき、状態変化を読み上げる。色のみで保留・失敗を表現しない。
