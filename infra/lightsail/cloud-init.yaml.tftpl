#cloud-config
package_update: true
package_upgrade: true

runcmd:
  - curl -fsSL https://get.docker.com | sh
  - usermod -aG docker ${deploy_user}
  - mkdir -p ${app_directory}
  - mkdir -p ${app_directory}/data
  - chown -R ${deploy_user}:${deploy_user} ${app_directory}
