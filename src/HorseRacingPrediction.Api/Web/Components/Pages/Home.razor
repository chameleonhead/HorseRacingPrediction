@page "/"
@attribute [Authorize]
@inject NavigationManager Nav

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (!firstRender) return;
        Nav.NavigateTo("/races", forceLoad: false, replace: true);
    }
}
